<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Educator Profile</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    body {
      background-color: #f4f6f9;
      font-family: 'Montserrat', sans-serif;
    }

    .sidebar {
      min-height: 100vh;
    }

    .sidebar .nav-link {
      font-weight: 500;
      color: #ddd;
      transition: 0.2s;
    }

    .sidebar .nav-link:hover {
      color: #1abc9c;
    }

    .sidebar .nav-link.active {
      color: #1abc9c;
    }

    .profile-header {
      display: flex;
      align-items: center;
      gap: 1rem;
    }

    .profile-header img {
      width: 100px;
      height: 100px;
      object-fit: cover;
      border-radius: 50%;
      border: 3px solid #0d6efd;
    }
  </style>
</head>

<body class="bg-light">
  <%- include("partials/nav-bar") %>

  <div class="container-fluid">
    <div class="row">
      <nav class="col-md-2 col-lg-2 sidebar">
        <%- include('partials/side-bar.ejs') %>
      </nav>

      <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 py-4">
        <h1 class="h2 mb-4">Educator Profile</h1>

        <div class="card shadow-sm p-4">
          <div class="profile-header mb-3">
            <img src="<%= profile && profile.avatar ? profile.avatar : '/images/default-avatar.jpg' %>" alt="Avatar" class="rounded-circle border border-3 border-primary">

            <div>
              <h3 class="mb-0"><%= user.username %></h3>
              <p class="text-muted mb-0">Role: <%= user.role %></p>
            </div>
          </div>

          <hr>

          <dl class="row mb-0">
            <dt class="col-sm-3">Email</dt>
            <dd class="col-sm-9"><%= user.email %></dd>

            <dt class="col-sm-3">Address</dt>
            <dd class="col-sm-9"><%= user.Profile?.address || '-' %></dd>

            <dt class="col-sm-3">Bio</dt>
            <dd class="col-sm-9"><%= user.Profile?.bio || 'No bio provided.' %></dd>
          </dl>

          <div class="mt-4">
            <a href="/profile/edit" class="btn btn-primary">
              <i class="bi bi-pencil-square"></i> Edit Profile
            </a>
          </div>
        </div>
      </main>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>